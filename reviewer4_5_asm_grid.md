# Reviewer4#5：ASM 网格与噪声条件分析

## 任务目标

当前阶段只分析 ASM 正问题的网格收敛性，确定一个足以获得稳定位移场且计算成本可接受的网格。反演和噪声敏感性分析暂不在本阶段执行，也不涉及 U-Net、final_model 或两种模型的公平比较。

## 执行步骤

1. 先统一节点数和单元数定义。本文统一按每个方向的节点数命名：20×20、40×40、80×80、200×200 节点分别对应 19×19、39×39、79×79、199×199 个单元。
2. 使用同一连续 BIL/EXP 模量场和同一个参考 GRF 模量场、载荷及边界条件测试 ASM 的 4×4、10×10、20×20、40×40、80×80、100×100、200×200 节点网格。
3. 以 200×200 节点结果作为正问题参考，记录位移场最大相对误差、最大绝对差异、求解时间和峰值内存。
4. 检查 BIL、EXP 与 GRF 的误差是否随网格细化稳定下降，并比较 80×80、100×100 与 200×200 的差异。
5. 选择误差已稳定且成本更低的最粗网格作为主比较网格。
6. 将后续反演阶段需要的噪声等级固定为 0%、2%、4%、6%、8%、10%，但本阶段不运行反演。

## 主要指标

```text
e_inf,ux(h) = max_y |u_x(h, y) - u_x(ref, y)| / max_y |u_x(ref, y)|
e_inf,uy(h) = max_y |u_y(h, y) - u_y(ref, y)| / max_y |u_y(ref, y)|
e_max(h)    = max(e_inf,ux(h), e_inf,uy(h))
```

定量比较必须基于同一个连续模量场、载荷和边界条件。不同网格的位移场只在需要展示或计算差值时插值到公共表示，不能用不同离散网格的节点索引直接比较。

## 输出物

```text
results/grid_study/
└── forward_demo/
    ├── bil/nodes_4...200/forward_result.pkl
    ├── exp/nodes_4...200/forward_result.pkl
    ├── grf/nodes_4...200/forward_result.pkl
    ├── forward_grid_metrics.csv
    ├── forward_edge_profiles.csv
    ├── forward_edge_*.{png,pdf}
    ├── forward_grid_convergence.{png,pdf}
    └── forward_solve_time.{png,pdf}
```

## Imported demo-data implementation

The three demo cases generated by the original data-generation workflow are
included inside the repository under `demo_distributions/`:

```text
bil/sample_600/true_modulus.npy
exp/sample_200/true_modulus.npy
grf/sample_410/true_modulus.npy
```

Each case contains measured displacement files for 0, 2, 4, 6, 8, and 10
percent noise. The importer keeps `target`, `ux`, `uy`, and the repository
compatible interleaved `U_measured_C`; it excludes legacy ASM predictions,
L-curve results, and U-Net outputs. `grid_study/demo_data.py` validates the
40 x 40 source shape and loads the source-generation metadata. BIL/EXP fields
are generated from the selected sample's `alpha` and `beta` on every target
mesh; the imported modulus field is used only for the 40 x 40 consistency
check. The original fixed-test-set indices are mapped to the corresponding
20,000-sample batch indices in the repository-level `demo_distributions/demo_cases.json`.

The demo forward study is self-contained and is run by directly executing:

```text
script/forward_grid_convergence.py
```

The default data root is the repository-local `demo_distributions`; an
external path is not required.

The demo inverse study regenerates clean displacements on each target mesh
from the parameterized true field, then applies the requested noise level on
that same mesh. The imported measured displacement files remain available as
40 x 40 reference data rather than being interpolated into other grids:

```text
python -m grid_study.inverse_convergence \
  --reference-nodes 40 --gamma <selected-gamma>
```

The inverse study remains intentionally blocked from automatic gamma
selection. Gamma must first be selected on the user-chosen reference grid,
then held fixed while comparing the other mesh densities.

每个 `bil/exp/grf/nodes_*` 目录还保存 `modulus_distribution`、`modulus_distribution_plain`、`displacement_fields`、`displacement_ux` 和 `displacement_uy` 的 PNG/PDF 图件。

## 正问题部分回复与当前实现

正问题网格收敛性分析已在 `grid_convergence` 分支实现，入口脚本为：

```text
python -m script.forward_grid_convergence
```

当前实现只调用仓库中的 ASM 正问题求解流程，不读取 U-Net、`final_model` 或其他反演模型。对 BIL、EXP 和 GRF 三种模量场，统一计算 4×4、10×10、20×20、40×40、80×80、100×100、200×200 节点网格，其中每个方向的单元数为节点数减 1。载荷、边界条件和材料参数均由仓库内三个 demo case 的元数据生成，并保持一致。

BIL 使用 `sample_600` 的 `alpha = -0.0423118`、`beta = 0.2127981`；EXP 使用 `sample_200` 的 `alpha = 0.1153648`、`beta = -0.0818312`。两种场均在每个目标网格上由各自的分布系数重新计算。

GRF 使用 `sample_410`，其 MATLAB 生成参数为 `E_max = 5.222611`、`sigma_g = 1.0`、`ell = 25 mm`、`jitter = 1e-6`、`seed_max = 4`、`batch_size = 20000`。代码从仓库内的 40×40 demo 模量场恢复该样本的高斯 realization 和等效 RBF 系数，再在每个目标网格上评价同一个 realization；不会在每个网格上重新抽取随机场。

200×200 节点结果作为参考解。不同网格的参考位移场通过 `RegularGridInterpolator` 插值到当前网格后，再计算 `u_x`、`u_y` 的最大相对误差及其最大值，避免直接比较不同离散网格的节点索引。精度曲线只显示 4×4 至 100×100 网格；200×200 是参考解，不在误差曲线上伪造零误差点。计算时间曲线保留 4×4 至 200×200 的全部网格。每个算例同时记录求解耗时和 Python 峰值内存；耗时先进行一次预热，再重复测量并取中位数，峰值内存单独测量，避免 4×4 等小网格受到首次库初始化和内存追踪开销的影响。

当前 BIL/EXP 结果中的最大相对位移误差如下；200×200 为参考解，不定义误差值：

```text
nodes       4           10          20          40          80          100         200
BIL      1.27e-1     2.44e-2     6.99e-3     1.96e-3     4.86e-4     2.88e-4      --
EXP      7.22e-2     1.03e-2     2.62e-3     6.72e-4     1.62e-4     9.54e-5      --
```

GRF 的最大相对位移误差如下。200×200 节点作为参考解，不定义误差值：

```text
nodes       4           10          20          40          80          100         200
GRF      2.79e-2     4.42e-3     1.19e-3     3.43e-4     7.68e-5     4.51e-5      --
```

80×80 与 200×200 结果已经较为接近：BIL、EXP 和 GRF 的最大相对位移误差分别约为 `4.86×10^-4`、`1.62×10^-4` 和 `7.68×10^-5`。在当前 demo 参数下，40×40 的 EXP 和 GRF 误差低于 `10^-3`，BIL 在 80×80 时低于 `10^-3`。因此，若三种场使用统一网格，80×80 已满足当前误差观察；100×100 可提供更大的精度余量。论文中应保留 40×40、80×80、100×100 和 200×200 的收敛证据，其中 200×200 用作参考位移解和计算时间点。

正问题输出包括：

- `forward_grid_metrics.csv`：网格误差、耗时和内存指标；
- `forward_edge_profiles.csv`：BIL/EXP/GRF 在右边界 `x = geo_l` 上的 `u_x(y)`、`u_y(y)` 位移剖面；
- `forward_grid_convergence.{png,pdf}`：双 y 轴网格误差与求解时间图；
- `forward_solve_time.{png,pdf}`：独立的正问题求解时间对数坐标图，显示 4×4 至 200×200 全部网格；
- `forward_edge_bil.{png,pdf}`、`forward_edge_exp.{png,pdf}` 和 `forward_edge_grf.{png,pdf}`：右边界 `u_x(y)`、`u_y(y)` 收敛曲线及 inset，三种场采用完全相同的绘图结构；
- 各网格目录下的 `forward_result.pkl` 与 `config.json`：可复核的正问题结果和算例配置。

本阶段尚未执行反演和噪声敏感性分析。后续反演阶段再使用 0%、2%、4%、6%、8%、10% 噪声等级，并在独立流程中评估噪声传播和反演稳定性。

## 反问题网格分析架构

反问题分析入口为 `grid_study/inverse_convergence.py`。当前只搭建和验证代码架构，不使用配置文件中的默认 `GAMMA` 直接形成正式反演结论。正确流程分两步：

1. 用户先在指定参考网格上运行现有 `script/inverse_l_curve.py`，或使用其他可说明的选参方法，确定参考网格上的 `gamma`。
2. 将该 `gamma` 和参考网格显式传给网格分析入口，在所有待比较网格上固定使用同一个值：

```text
python -m grid_study.inverse_convergence \
  --reference-nodes 40 \
  --gamma 1e-6 \
  --datasets bil exp grf \
  --nodes 4 10 20 40 80 100 \
  --noise-percentage 0 2 4 6 8 10 \
  --resume
```

入口不会自动重新选择 `gamma`，这样可以把网格尺寸影响与正则化参数选择影响分离。目录按参考项目组织为 `dataset/nodes_<N>/noise_<percentage>`，例如 `bil/nodes_40/noise_6`；噪声目录位于各自网格目录之下。每个 case 独立保存正问题输入、反演结果、配置和标量指标，支持中断后用 `--resume` 续跑。输出指标包括模量重建相对 L1/L2/L∞ 误差、位移拟合误差、噪声水平、迭代次数、收敛状态、求解时间和峰值内存，并生成精度、时间和内存三类 PDF/PNG 图。

`200×200` 不放入默认反演矩阵，因为反演未知量数量按节点数平方增长；如确有必要，可通过命令行显式加入，但应单独评估内存和耗时。当前尚未运行上述正式反问题矩阵，也没有据此给出网格选择结论。

## 项目交接说明（复制到新对话）

```text
你在 D:\MyProject\U-Net\ASM-Python 的网格收敛分支工作。
请先阅读 ASM-Python 的 README.md 和 reviewer4_5_asm_grid.md，检查 ASM 路径和网格定义。
本阶段只分析 ASM 正问题网格收敛：4×4、10×10、20×20、40×40、80×80、100×100、200×200 节点，覆盖 BIL、EXP 和 GRF；以 200×200 节点为参考，GRF 使用从 `sample_410` 恢复的固定 RBF realization 在各网格上评价，记录最大相对位移误差、运行时间和内存。精度图显示 4×4 至 100×100，计算时间图显示 4×4 至 200×200。噪声等级 0%、2%、4%、6%、8%、10% 留给后续反演阶段。不读取、不比较 U-Net 或 final_model。先做小规模 CPU smoke test，不要覆盖正式结果。
```
