# Reviewer4#5：ASM 网格与噪声条件分析

## 任务目标

当前阶段只分析 ASM 正问题的网格收敛性，确定一个足以获得稳定位移场且计算成本可接受的网格。反演和噪声敏感性分析暂不在本阶段执行，也不涉及 U-Net、final_model 或两种模型的公平比较。

## 执行步骤

1. 先统一节点数和单元数定义。本文统一按每个方向的节点数命名：20×20、40×40、80×80、200×200 节点分别对应 19×19、39×39、79×79、199×199 个单元。
2. 使用同一连续 BIL/EXP 模量场和同一个参考 GRF 模量场、载荷及边界条件测试 ASM 的 4×4、10×10、20×20、40×40、80×80、100×100、200×200 节点网格。
3. 以 200×200 节点结果作为正问题参考，记录位移场相对 L1/L2 差异、最大绝对差异、求解时间和峰值内存。
4. 检查 BIL、EXP 与 GRF 的误差是否随网格细化稳定下降，并比较 80×80、100×100 与 200×200 的差异。
5. 选择误差已稳定且成本更低的最粗网格作为主比较网格。
6. 将后续反演阶段需要的噪声等级固定为 0%、2%、4%、6%、8%、10%，但本阶段不运行反演。

## 主要指标

```text
E_true(h) = ||u(h) - u_true||_1 / ||u_true||_1
D_ref(h)  = ||u(h) - I_h(u(100))||_1 / ||I_h(u(100))||_1
```

定量比较必须基于同一个连续模量场、载荷和边界条件。不同网格的位移场只在需要展示或计算差值时插值到公共表示，不能用不同离散网格的节点索引直接比较。

## 输出物

```text
results/grid_study/
└── forward/
    ├── bil/nodes_4...200/forward_result.pkl
    ├── exp/nodes_4...200/forward_result.pkl
    ├── grf/nodes_4...200/forward_result.pkl
    ├── forward_grid_metrics.csv
    ├── forward_edge_profiles.csv
    ├── forward_edge_*.{png,pdf}
    └── forward_grid_convergence.{png,pdf}
```

## 正问题部分回复与当前实现

正问题网格收敛性分析已在 `grid_convergence` 分支实现，入口脚本为：

```text
python -m script.forward_grid_convergence
```

当前实现只调用仓库中的 ASM 正问题求解流程，不读取 U-Net、`final_model` 或其他反演模型。对 BIL、EXP 和 GRF 三种模量场，统一计算 4×4、10×10、20×20、40×40、80×80、100×100、200×200 节点网格，其中每个方向的单元数为节点数减 1。载荷、边界条件和材料参数均由现有正问题配置生成，并保持一致。

GRF 生成在仓库中按单个 case 实现，不保留 MATLAB 数据集批量接口。网格收敛分析先在 200×200 参考网格上根据 `GRF_E_MAX`、`GRF_SIGMA_G`、`GRF_ELL` 和 `GRF_SEED` 生成一次二维节点场，再用 `RegularGridInterpolator` 将该场插值到其他网格。因此，不同网格之间使用的是同一个 GRF realization，误差反映离散网格变化，而不是随机场变化。

200×200 节点结果作为参考解。不同网格的参考位移场通过 `RegularGridInterpolator` 插值到当前网格后，再计算相对 L1/L2 差异和最大绝对差异，避免直接比较不同离散网格的节点索引。每个算例同时记录求解耗时和 Python 峰值内存；耗时先进行一次预热，再重复测量并取中位数，峰值内存单独测量，避免 4×4 等小网格受到首次库初始化和内存追踪开销的影响。

当前 BIL/EXP 结果中的相对 L2 位移差异如下：

```text
nodes       4           10          20          40          80          100         200
BIL      3.64e-2     5.16e-3     1.30e-3     3.32e-4     7.76e-5     4.56e-5     0
EXP      3.29e-2     4.10e-3     9.82e-4     2.43e-4     5.62e-5     3.32e-5     0
```

GRF 的相对 L2 位移差异如下。GRF 使用 `E_max = 8.0`、`sigma_g = 1.0`、`ell = 25.0 mm`、`seed = 42`，并在 200×200 节点参考网格上生成一次后插值到各网格：

```text
nodes       4           10          20          40          80          100         200
GRF      4.36e-3     6.31e-4     1.98e-4     1.56e-4     3.33e-5     2.86e-5     0
```

80×80 与 200×200 结果已经非常接近：BIL、EXP 和 GRF 的相对 L2 差异分别约为 `7.76×10^-5`、`5.62×10^-5` 和 `3.33×10^-5`。同时，80×80 的稳态求解时间约为 `0.166 s`，200×200 约为 `1.42 s`；峰值 Python 内存约为 `11.0 MB` 和 `69.8 MB`。在当前 GRF 参数 `ell = 25.0 mm` 下，40×40 节点的相对 L2 差异约为 `1.56×10^-4`，低于 `10^-3`，因此 40×40 可以作为文章中的候选主网格；若需要更保守的网格误差裕量，则使用 80×80。建议在论文中保留 40×40、80×80、100×100 和 200×200 的收敛证据，不要只报告单个 40×40 结果。

正问题输出包括：

- `forward_grid_metrics.csv`：网格误差、耗时和内存指标；
- `forward_edge_profiles.csv`：BIL/EXP/GRF 在右边界 `x = geo_l` 上的 `u_x(y)`、`u_y(y)` 位移剖面；
- `forward_grid_convergence.{png,pdf}`：双 y 轴网格误差与求解时间图；
- `forward_edge_bil.{png,pdf}`、`forward_edge_exp.{png,pdf}` 和 `forward_edge_grf.{png,pdf}`：右边界 `u_x(y)`、`u_y(y)` 收敛曲线及 inset，三种场采用完全相同的绘图结构；
- 各网格目录下的 `forward_result.pkl` 与 `config.json`：可复核的正问题结果和算例配置。

本阶段尚未执行反演和噪声敏感性分析。后续反演阶段再使用 0%、2%、4%、6%、8%、10% 噪声等级，并在独立流程中评估噪声传播和反演稳定性。

## 项目交接说明（复制到新对话）

```text
你在 D:\MyProject\U-Net\ASM-Python 的网格收敛分支工作。
请先阅读 ASM-Python 的 README.md 和 reviewer4_5_asm_grid.md，检查 ASM 路径和网格定义。
本阶段只分析 ASM 正问题网格收敛：4×4、10×10、20×20、40×40、80×80、100×100、200×200 节点，覆盖 BIL、EXP 和 GRF；以 200×200 节点为参考，GRF 先生成一个参考场再插值到各网格，记录位移场差异、运行时间和内存。噪声等级 0%、2%、4%、6%、8%、10% 留给后续反演阶段。不读取、不比较 U-Net 或 final_model。先做小规模 CPU smoke test，不要覆盖正式结果。
```
